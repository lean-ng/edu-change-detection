import { HighlightCheckDirective } from './highlight-check.directive';

describe('HighlightCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCheckDirective();
    expect(directive).toBeTruthy();
  });
});
